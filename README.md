# Supplementary Code: A Fast, Closed-Form Bandwidth Selector for the Beta Kernel Density Estimator

This repository contains the Python code and data to reproduce all
experiments, tables, and figures in the paper *"A Fast, Closed-Form Bandwidth
Selector for the Beta Kernel Density Estimator"*, submitted to the *Journal of
Computational and Graphical Statistics*.

**Requirements:** Python >= 3.11 and an internet connection (for the UCI
dataset used in Experiment 2).

## Quick Start

Reproducing every table, figure, and supplementary CSV from the pre-computed
data takes under a minute:

```bash
# 1. Clone the repository
git clone <repository-url>
cd beta-kernel-reproduce-paper

# 2. Install dependencies (pick one)
uv sync                          # recommended — uses the exact lockfile
# or: pip install -r requirements.txt

# 3. Reproduce all outputs
uv run python reproduce_all.py   # if using uv
# or: python reproduce_all.py
```

All generated files are written to `output/` (tables, plots, and supplementary
CSVs). On a typical laptop the script finishes in under a minute. The
`output/` directory is created automatically.

## Output-to-Paper Mapping

The table below shows exactly which output file corresponds to each table and
figure in the paper.

### Tables

| Paper Reference | Output File | Generated By |
|-----------------|-------------|--------------|
| Table 1 | `output/tables/main_table_lscv.tex` | `scripts/tables/tables_experiment_1.py` |
| Table 2 | `output/tables/main_table_ise.tex` | `scripts/tables/tables_experiment_1.py` |
| Table 3 | `output/tables/main_table_time.tex` | `scripts/tables/tables_experiment_1.py` |
| Table 4 | `output/tables/experiment_2_table.tex` | `scripts/tables/table_experiment_2.py` |
| Appendix C, Table 1 | `output/tables/ablation_table.tex` | `scripts/tables/table_ablation.py` |

### Figures

| Paper Reference | Output File | Generated By |
|-----------------|-------------|--------------|
| Figure 1 | `output/plots/kernel_shape_plot.pdf` | `scripts/plot/plot_kernel_shapes.py` |
| Figure 2 | `output/plots/LSCV_Score_vs_N.pdf` | `scripts/plot/plot_experiment_1.py` |
| Figure 3 | `output/plots/ISE_Score_vs_N.pdf` | `scripts/plot/plot_experiment_1.py` |
| Figure 4 | `output/plots/Comp_Time_vs_N.pdf` | `scripts/plot/plot_experiment_1.py` |
| Figure 5 | `output/plots/Bandwidth_vs_N_NiceDists.pdf` | `scripts/plot/plot_experiment_1.py` |
| Figure 6 | `output/plots/experiment_2_visual_fits.pdf` | `scripts/plot/plot_experiment_2.py` |
| Appendix B, Figure 1 | `output/plots/IntegralError_vs_N.pdf` | `scripts/plot/plot_experiment_1.py` |
| Appendix C, Figure 2 | `output/plots/ablation_delta_plot.pdf` | `scripts/plot/plot_ablation.py` |

### Supplementary Material

| Output File | Generated By |
|-------------|--------------|
| `output/supplementary/supplementary_experiment_1_results.csv` | `scripts/export/export_experiment_1.py` |
| `output/supplementary/supplementary_experiment_2_results.csv` | `scripts/export/export_experiment_2.py` |

## Installation

Requires **Python >= 3.11**.

### Using uv (recommended)

[uv](https://docs.astral.sh/uv/) is a fast Python package manager. The
repository ships a `uv.lock` lockfile that pins every dependency to the exact
versions used during development.

```bash
uv sync
```

### Using pip

A traditional `requirements.txt` is also provided:

```bash
pip install -r requirements.txt
```

## Repository Structure

```
├── KDE.py                          # Beta kernel density estimator (Chen, 1999)
├── KDE_Gauss.py                    # Gaussian KDE with boundary corrections
├── reproduce_all.py                # One-command reproduction of all outputs
├── pyproject.toml                  # Project metadata & dependencies (uv / pip)
├── requirements.txt                # Pinned dependencies (pip fallback)
├── uv.lock                        # Exact dependency lockfile (uv)
├── LICENSE                         # BSD-3-Clause license
├── scripts/
│   ├── _paths.py                   # Root-path resolution & output-dir creation
│   ├── run/                        # Experiment scripts (computationally intensive)
│   │   ├── run_experiment_1.py     #   Monte Carlo simulation (Experiment 1)
│   │   ├── run_experiment_2.py     #   Real-data cross-validation (Experiment 2)
│   │   └── run_ablation_study.py   #   Ablation study
│   ├── tables/                     # LaTeX table generation
│   │   ├── tables_experiment_1.py  #   Tables 1–3
│   │   ├── table_experiment_2.py   #   Table 4
│   │   └── table_ablation.py       #   Appendix C, Table 1
│   ├── plot/                       # Figure generation
│   │   ├── _plot_styles.py         #   Shared colour palette & line-style theme
│   │   ├── plot_experiment_1.py    #   Figures 2–5, Appendix B Fig. 1
│   │   ├── plot_experiment_2.py    #   Figure 6
│   │   ├── plot_ablation.py        #   Appendix C, Figure 2
│   │   └── plot_kernel_shapes.py   #   Figure 1
│   └── export/                     # Supplementary CSV export
│       ├── export_experiment_1.py
│       └── export_experiment_2.py
├── data/                           # Pre-computed experiment results (committed)
│   ├── experiment1/
│   ├── experiment2/
│   └── ablation_study/
└── output/                         # Generated artifacts (created automatically)
    ├── tables/
    ├── plots/
    └── supplementary/
```

## Pre-computed Data

The `data/` directory contains pre-computed results so that all tables and
figures can be reproduced without re-running the experiments:

| File | Description |
|------|-------------|
| `data/experiment1/simulation_results_full.csv` | Raw Experiment 1 simulation results (1000 replications × 7 distributions × 6 sample sizes × 10 methods) |
| `data/experiment2/experiment_2_summary.csv` | Experiment 2 summary statistics |
| `data/experiment2/per_fold/experiment_2_per_fold_results.csv` | Experiment 2 per-fold cross-validation results |
| `data/ablation_study/ablation_results.csv` | Ablation study results |

## Reproducing Tables and Figures

> **Note:** All plotting scripts use distinct line styles (dashed, dotted) and
> markers so that figures are fully interpretable when printed in black and
> white, per JCGS guidelines.

> **Note on `uv`:** If you installed dependencies with `uv sync`, prefix all
> `python` commands below with `uv run` (e.g., `uv run python reproduce_all.py`).

### All at once

```bash
python reproduce_all.py
```

This runs every table, plot, and export script in sequence and writes output
to `output/tables/`, `output/plots/`, and `output/supplementary/`.

### Individual scripts

```bash
# --- Tables ---
python scripts/tables/tables_experiment_1.py   # Tables 1–3
python scripts/tables/table_experiment_2.py    # Table 4
python scripts/tables/table_ablation.py        # Appendix C, Table 1

# --- Figures ---
python scripts/plot/plot_kernel_shapes.py      # Figure 1
python scripts/plot/plot_experiment_1.py       # Figures 2–5, Appendix B Fig. 1
python scripts/plot/plot_experiment_2.py       # Figure 6 (requires internet)
python scripts/plot/plot_ablation.py           # Appendix C, Figure 2

# --- Supplementary CSVs ---
python scripts/export/export_experiment_1.py
python scripts/export/export_experiment_2.py
```

## Re-running the Experiments (Optional)

> **Warning:** The full experiment pipeline is computationally intensive.
> Experiment 1 takes approximately 28 hours on a 32-core workstation.
> Pre-computed results are already provided in `data/`, so re-running is
> **not required** to reproduce the paper's tables and figures.

```bash
# Experiment 1 — Monte Carlo simulation (parallelised; set MAX_WORKERS in script)
python scripts/run/run_experiment_1.py

# Experiment 2 — real-data cross-validation (downloads UCI data automatically)
python scripts/run/run_experiment_2.py

# Ablation study (parallelised)
python scripts/run/run_ablation_study.py
```

Experiment 1 supports **resumption**: if interrupted, re-running the script
skips already-completed trials.

After re-running, regenerate all outputs with:

```bash
python reproduce_all.py
```

## License

This project is licensed under the BSD-3-Clause License — see the
[LICENSE](LICENSE) file for details.